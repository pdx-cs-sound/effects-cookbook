# Compression

> Dynamic range compression turns down the loud parts of a signal, shrinking the gap
> between loud and quiet. The track sits more evenly and can be raised in overall level.

*Chapter 6 — companding. See also [Limiting](limiter.md), [Expanding](expander.md), the
slower, target-seeking [AGC](agc.md), and the clock-driven [Tremolo](tremolo.md) of
Chapter 5.*

---

## Intuition

A compressor behaves like a sound engineer with one hand on a volume fader: the fader
comes down the moment the singer gets too loud and comes back up when the singer drops
back. The compressor automates the hand. The threshold sets how loud is too loud; the
ratio sets how hard the fader comes down.

Typical uses: evening out an erratic performance, taming peaks so the overall level can be
raised, gluing a mix together, adding punch or sustain.

## Key parameters

| Parameter | What it controls |
|---|---|
| Threshold | The level above which compression begins (dBFS). |
| Ratio | How much the overshoot is reduced. At 4:1, 4 dB over the threshold comes out as 1 dB. |
| Attack | How fast the gain comes down once the threshold is crossed (ms). |
| Release | How fast the gain recovers once the signal drops back (ms). |
| Knee | How abruptly the ratio engages around the threshold (hard vs. soft). |
| Makeup gain | Fixed boost after compression to restore overall level (dB). |

## How it works

1. Detect the level of the incoming signal (peak or RMS).
2. Compare with the threshold. Below it, do nothing. Above it, compute the gain reduction
   from the ratio.
3. Smooth the gain change with the attack and release time constants.
4. Apply the smoothed gain to the signal, then add makeup gain.

Two figures, generated by running this book's compressor (`code/make_figures.py`):

![Steady-state transfer curves. Below the −20 dBFS threshold every ratio follows the unity line; above it, 2:1 and 4:1 reduce the slope, and an infinite ratio holds the output flat at the threshold.](img/compression_transfer.svg)

*The static transfer function: what the ratio does to steady-state level. Every curve is
unity below the threshold. The ∞:1 curve is a limiter.*

![A quiet–loud–quiet tone through the compressor at 4:1. The gain-reduction trace falls at the loud section's onset and recovers after it ends; the output envelope is drawn bold only while gain reduction is active.](img/compression_gain_reduction.svg)

*The same compressor in time: a quiet–loud–quiet tone at 4:1, −20 dBFS threshold, 5 ms
attack, 50 ms release. The gain trace falls at the onset (attack) and recovers after the
loud section ends (release). The output is drawn bold only where the compressor is
reducing gain; elsewhere it equals the input.*

## Pseudocode

```text
COMPRESS(x, threshold, ratio, attack, release, makeup)
    gain ← 0
    for each sample s in x
        level ← DETECT(s)                          ▷ peak or RMS, in dBFS
        if level > threshold
            target ← (threshold − level) · (1 − 1/ratio)
        else
            target ← 0
        gain ← SMOOTH(gain, target, attack, release)
        emit s · LINEAR(gain + makeup)
```

DETECT reports a level in dBFS, SMOOTH is the one-pole follower of
[Chapter 5](envelopes.md), and LINEAR converts a dB value to a linear gain. The pseudocode
on the other pages of this chapter uses the same subroutines.

## Reference implementation

Building a compressor involves more decisions than the pseudocode above shows: a detector,
a topology, a knee shape, a place to smooth, lookahead, and makeup. The map below starts
from no design; picking one option at each decision traces a path, and every path ends at
a complete compressor. The toggles overlay five open-source implementations, each taking a
different route. Context on the five projects is in the [References](references.md)
appendix.

<iframe src="../visualization/compressor_decision_map.html" title="Compressor design decision flow with five implementation paths" loading="lazy" style="width:100%; height:820px; border:1px solid #d8d8d2; border-radius:8px;"></iframe>

Two facts the map understates. Not every path composes: a feedback topology cannot look
ahead at output that does not exist yet. And the map's resolution is itself a design
choice: the options listed are the coarsest set that still separates all five reference
implementations.

A configurable implementation whose keyword arguments are the rows of the map lives in the
repository at
[`code/compressor.py`](https://github.com/pdx-cs-sound/effects-cookbook/blob/main/code/compressor.py).
The five reference implementations are included as presets;
`compress(x, sr, **PRESETS["audacity"])` runs their path. The engine is exercised by
`code/test_compressor.py`, and the time-domain figures in this chapter are generated by
running it (`code/make_figures.py`).

!!! warning "Pitfalls"
    - Pumping and breathing: a release that is too fast makes the level surge audibly
      between hits.
    - dB vs. linear: gain math lives in dB; sample math lives in linear. Convert
      deliberately.
    - Lookahead: a feed-forward detector reacts after a transient begins. Limiters add
      lookahead to catch it; see [Limiting](limiter.md).

## Related effects

- [Automatic Gain Control](agc.md): the slower, target-seeking member of the family.
- [Limiting](limiter.md): compression with an infinite ratio and a fast attack.
- [Expanding](expander.md), and the gate: the inverse; they turn down the quiet parts.

## Learn more

- Zölzer & Holters, *Digital Audio Effects*.
