# Meeting feedback, 2026-07-13

Raw list from the meeting, filed before execution. Work proceeds largest blast radius
first: structure, then stack enablers, then general content, then specifics, with a final
tone/adversarial pass over everything.

## Structure

- [x] Move envelopes and tremolo to a new Chapter 5 (envelopes leave the waveforms page
      and become their own page).
- [x] Renumber: everything after the new Chapter 5 moves up one (11 chapters total).
- [x] New Chapter 6, "Companding": compression, limiting, expanding, AGC. (The title
      returns; the mu-law collision that retired it is dissolved by dropping mu-law.)
- [x] Remove the compressor design page. The decision-map visualization moves to the
      compression page; the full source becomes a link to `code/compressor.py` instead of
      an embedded listing.

## Tone — still reads "like AI" in places

- [ ] Reduce idioms further.
- [ ] Adversarial check: Ed will run an AI-detector on the prose. Before that, a
      self-adversarial sweep hunting AI tells across all pages.
- [ ] Complete sentences; colons, semicolons, and dashes should be rare. (STYLE.md rule.)

## General

- [x] (stack enabled; equations themselves pending) Math as math: real equations (sine definition, y = f(x), the dB formulas), not code
      blocks. Needs MathJax/arithmatex in the stack.
- [ ] Pseudocode in the style of an algorithms textbook, not informal Python. (Settles the
      long-open pseudocode-conventions TBD.)
- [x] Navigation bars at the top and bottom of each page.
- [ ] Chapter 2: sample rate is samples per second, not a frequency in the tonal sense;
      say so where "Hz" is introduced.

## Specifics

- [ ] Bit crush: bucket count need not be a power of two; powers of two are just likely.
- [x] Mu-law: dropped; archived in `research/dropped-mu-law.md`.
- [ ] Distortion: add asymmetric clipping and its harmonic effects.
- [ ] Sawtooth: rising vs. falling sound the same to the ear until it is a very slow LFO.
- [x] Tremolo: the tremolo-vs-vibrato clarification moves to the top of the page.
- [ ] AGC: real AGCs generally trigger fast and release slow; the current page and
      implementation present one slow time constant. Validate against legitimate
      references (Ed owns finding them; note Woodgate EN 27.1 already says attack of the
      order of milliseconds with release of one second or more).
- [ ] Chapter 2: note that stereo effects are mostly the mono effect per channel, hence
      the mono-only scope.
- [ ] Chapter 2: treat peak-to-peak amplitude explicitly (what it is, where it is a
      legitimate measurement, why it is not a level), beyond the current warning.
