# Meeting feedback, 2026-07-13

Raw list from the meeting, filed before execution. Work proceeds largest blast radius
first: structure, then stack enablers, then general content, then specifics, with a final
tone/adversarial pass over everything.

## Structure

- [x] Move envelopes and tremolo to a new Chapter 5 (envelopes leave the waveforms page
      and become their own page).
- [x] Renumber: everything after the new Chapter 5 moves up one (11 chapters total).
- [x] New Chapter 6, "Companding": compression, limiting, expanding, AGC. (The title
      returns; the mu-law collision that retired it is dissolved by dropping mu-law.)
- [x] Remove the compressor design page. The decision-map visualization moves to the
      compression page; the full source becomes a link to `code/compressor.py` instead of
      an embedded listing.

## Tone — still reads "like AI" in places

- [ ] Reduce idioms further.
- [ ] Adversarial check: Ed will run an AI-detector on the prose. Before that, a
      self-adversarial sweep hunting AI tells across all pages.
- [ ] Complete sentences; colons, semicolons, and dashes should be rare. (STYLE.md rule.)

## General

- [x] Math as math: dB formulas, dBFS, sine definition, y = f(x), per-effect one-liners
      all converted (arithmatex + MathJax).
- [x] Pseudocode in the style of an algorithms textbook (COMPRESS/LIMIT/EXPAND/AGC
      procedures; ← , for each, ▷ comments, named subroutines). Convention recorded in
      DESIGN §3.
- [x] Navigation bars at the top and bottom of each page.
- [x] Chapter 2: sample rate is samples per second, not a frequency in the tonal sense.

## Specifics

- [x] Bit crush: bucket count generalized to any level count; powers of two noted as the
      common case (bit depths).
- [x] Mu-law: dropped; archived in `research/dropped-mu-law.md`.
- [x] Distortion: asymmetric clipping added (code, figure curve, even-harmonics prose
      with the tube-stage reference, hedged per rule 8).
- [x] Sawtooth: rising vs. falling paragraph added (identical spectra as tones; direction
      matters at LFO rates).
- [x] Tremolo: the tremolo-vs-vibrato clarification moves to the top of the page.
- [x] AGC: reworked to fast attack / slow release throughout (params, mechanism,
      pseudocode, implementation with separate detector envelope and two rates; Woodgate
      cited in prose). Ed still owns confirming with additional references.
- [x] Chapter 2: mono-only scope explained ("Why mono only?" note; per-channel + shared
      detector remark).
- [x] Chapter 2: peak-to-peak promoted from a warning to its own subsection (what it is,
      where it is legitimate, why it is not a level).
